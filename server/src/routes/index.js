// TODO: Register all sub-routers under api namespace (e.g. /api/auth, /api/restaurants, /api/reviews, /api/aws, /health).
import { Router } from 'express';

const router = Router();

export default router;
