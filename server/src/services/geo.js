// TODO: Compute distance between user GPS and restaurant coordinates using Haversine formula (Success if distance <= 200m).
export class GeoService {
  // Haversine calculation helper methods placeholder
}
export const geo = new GeoService();
