// TODO: Extract text via Textract, compute Levenshtein distance >= 80% for merchant name, and validate invoice 48h limit.
export class OcrService {
  // Levenshtein and invoice dates checks helper methods placeholder
}
export const ocr = new OcrService();
