/**
 * ReviewModel
 * TODO: Define reviews database table interface (e.g. reviews).
 * Fields: id, user_id, restaurant_id, rating, content, invoice_hash (SHA-256), is_verified_gps, is_verified_ocr, trust_coefficient, created_at.
 */
export class ReviewModel {
  // Database entity interface placeholder
}
