// TODO: Expose S3 presigned URLs, scan invoices with AWS Textract (OCR), and trigger AWS Bedrock (Claude review summaries).
export const getUploadUrl = async (req, res, next) => {
  // Presigned S3 put link placeholder
};

export const scanInvoice = async (req, res, next) => {
  // Textract OCR scan and Levenshtein comparison placeholder
};

export const summarizeReviews = async (req, res, next) => {
  // Bedrock summarization API trigger placeholder
};
