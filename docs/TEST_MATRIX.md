# Test Matrix

This file maps TrustBite product behavior to proof. The durable source used by agents is also the Harness CLI:

```bash
npm run harness -- query matrix
```

Do not mark a row `implemented` until code exists and validation evidence is recorded.

## Status Values

| Status | Meaning |
| --- | --- |
| planned | Accepted as intended behavior, not implemented |
| in_progress | Actively being built |
| implemented | Implemented and proof exists |
| changed | Contract changed after earlier implementation |
| retired | No longer part of the product contract |

## Matrix

| Story | Contract | Unit | Integration | E2E | Platform | Status | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TB-HARNESS-001 | Repository has Harness operating docs, templates, durable CLI database, portable npm wrapper, and project-specific agent rules | no | yes | no | yes | implemented | `npm run harness -- query matrix`; `npm run harness -- story verify TB-HARNESS-001` |
| TB-REST-001 | Restaurant CRUD API persists restaurants, keeps geo/category data consistent, and soft-deletes safely | yes | yes | no | yes | implemented | 2026-07-08 Phase 3 backend CRUD closeout: db:migrate applied 0; unit proof passed 13 files / 115 tests; restaurant CRUD integration proof passed 6 files / 49 tests with 1 skipped file / 2 skipped tests; server:build passed 91 files; story verify passed; decision 0016 records the mutation-boundary policy. |
| TB-REST-002 | Public restaurant list supports keyword, radius, trust-score filter, sort, and nearby map-bounds lookup | yes | yes | no | yes | implemented | 2026-07-08 Phase 3 search/filter/nearby closeout: db:migrate applied 0; unit proof passed 13 files / 115 tests; restaurant search integration proof passed 7 files / 63 tests with 1 skipped file / 2 skipped tests; server:build passed 91 files; story verify passed. |
| REST-US-003 | Public restaurant detail returns active profile, rating breakdown, latest claim status, and verified/reference public reviews | yes | yes | no | yes | implemented | 2026-07-08 Phase 3 detail/reviews closeout: db:migrate applied 0; unit proof passed 13 files / 115 tests; restaurant detail integration proof passed 8 files / 67 tests with 1 skipped file / 2 skipped tests; server:build passed 91 files; story verify passed. |
| TB-DATA-003 | Restaurant soft delete uses `is_deleted` and `deleted_at` without conflating business `status = 'CLOSED'` | no | yes | no | yes | implemented | 2026-07-08 backend/data closeout: db:migrate applied 0; targeted restaurant controller/CRUD proof passed 2 files / 14 tests; server:build passed 104 files; story verify passed; integration proof covers DELETE setting `is_deleted`/`deleted_at`, repeated DELETE 404, GET/list/PATCH exclusion for soft-deleted rows, and non-deleted `status='CLOSED'` remaining distinct from deletion. UI/client/mobile/admin UI excluded. |
| TB-APP-001 | TrustBite app can be installed and both client/server development processes are documented | no | yes | no | yes | implemented | 2026-07-08 app smoke closeout: `npm run verify:tb-app-smoke` confirmed workspace scripts, package-lock files, client/server `node_modules`, then ran `npm run client:build` successfully for static routes `/` and `/_not-found` plus `npm run server:build` with 103-file syntax proof; `npm run harness -- story verify TB-APP-001` passed through the same command. |
| TB-USER-AVATAR-UPLOAD-001 | Implement avatar upload URL endpoint | yes | yes | no | no | implemented | 2026-07-09 cleanup allowlist review fix: avatar upload now validates the returned public `avatarUrl` through the same owned-object parser used by account-deletion S3 cleanup before signing; custom CDN hosts must be present in both the avatar allowlist and cleanup allowlist or the endpoint fails closed with `PROVIDER_UNAVAILABLE`. Red unit proof failed on unapproved cleanup host; `npm run verify:tb-user-avatar-upload` passed with `db:migrate` applied 0, targeted unit proof 21 files / 222 tests, targeted profile integration proof 12 files / 2 skipped and 103 tests / 4 skipped, and `server:build` 104 files. `npm run harness -- story verify TB-USER-AVATAR-UPLOAD-001` passed; deterministic signer test double used, live S3 PUT not claimed. |
| TB-REST-IMAGE-001 | ADMIN/SUPER_ADMIN uploads a validated restaurant image to private S3, persists an owned reference, returns short-lived signed GET URLs, and atomically replaces the public primary image | yes | yes | no | yes | in_progress | 2026-07-14: signed-delivery focused proof passed 40 tests; private bucket and runtime bucket/TTL configuration are present. Live AWS E2E awaits least-privilege `PutObject/GetObject/DeleteObject` IAM permission and an admin upload smoke test; CloudFront is not required. |
| TB-UI-001 | Home page shows restaurant search/list/detail and anti-fraud simulation for receipt/GPS workflow | no | no | planned | no | planned | `client/src/app/page.js`; needs client build and manual/E2E proof |
| TB-API-001 | Express server exposes health and API namespace with mounted auth/restaurant/review/aws routes | no | planned | no | no | planned | `server/src/app.js`, `server/src/routes/`; route mounting incomplete |
| TB-AUTH-001 | Cognito-first authentication contract; Express business APIs verify Cognito JWTs and enforce local account state | planned | planned | planned | no | planned | high-risk; no proof yet |
| TB-REVIEW-001 | Users can create verified food reviews tied to restaurant, receipt, and verification state | yes | yes | no | no | implemented | 2026-07-08 Phase 4 backend Review & OCR closeout plus PR #38 review follow-up: PR #36/TB-FRAUD-001 dependency verified merged into `main`; queue enqueue failures now persist a fixed public manual-review reason instead of raw queue/provider error messages exposed by owner-scoped status metadata; db:migrate applied 0; full server test passed 31 files / 1 skipped and 308 tests / 2 skipped; server:build passed 102 files; story verify passed. Covers 4.1 review create API, 4.2 receipt upload/private S3/idempotency/duplicate hash proof, 4.3 upload-to-OCR enqueue including durable `PENDING_ADMIN_REVIEW` degradation when post-commit enqueue fails, 4.4 receipt verification lifecycle proof through PR #36, and 4.5 owner-scoped review status API. UI/mobile/admin UI excluded. |
| TB-FRAUD-001 | Receipt OCR verification rejects duplicates, validates merchant/timestamp, and stores evidence | yes | yes | no | no | implemented | 2026-07-08 PR #36 merged into `main`; Harness marks implemented with OCR provider/unit/integration proof and full story verify evidence. Live AWS Textract E2E is not claimed. |
| TB-FRAUD-002 | GPS/Haversine verification validates user proximity to restaurant using explicit threshold | yes | no | no | no | implemented | Backend GPS/Haversine unit proof exists with explicit configurable threshold; UI simulation is not the source of truth. |
| TB-TRUST-001 | Restaurant `trust_score` is computed by backend rules from reviews, weighted by `trust_weight_bucket` and reviewer rank (Anti-Fraud §10) | yes | no | no | no | in_progress | 2026-07-10: pure `computeTrustScore`/`weightForReview` calculator + `recomputeRestaurantTrustScore` service delivered with unit proof (`tests/unit/trust/` 2 files / 19 tests; `npm run test:unit` 23 files / 251 tests; `server:build` 107 files). Decision 0018. No schema change. Live DB proof PASS 2026-07-10 (recompute joined a transaction: trust_score 4.64 + counts persisted, NONE excluded, rolled back, 0 residue). Deferred: wiring recompute into verification/admin/deletion triggers. Earlier "implemented" row was corrected — no recompute service existed before this slice. |
| TB-GAMIFY-001 | Authenticated gamification summary API returns EXP, derived level + progress, and badges | yes | no | no | no | in_progress | 2026-07-10: `gamificationRules` ladder + pure `resolveRank` + `getUserGamification` read service + `GET /users/me/gamification` (auth) delivered with unit proof (`tests/unit/gamification/` 2 files / 13 tests; `npm run test:unit` 25 files / 264 tests; `server:build` 110 files). Decision 0019. No schema change. Deferred: EXP awarding writes + `rank_code` persistence wired into review/verification, badge-award triggers (P1), and live route/DB proof (Docker down). |
| TB-AWS-001 | AWS integrations work locally through LocalStack where supported and are isolated behind services | yes | yes | no | yes | implemented | 2026-07-08 local provider-boundary closeout: red S3 unit proof showed `LOCALSTACK_ENDPOINT_URL` did not reach `awsConfig.s3.endpoint`; red SES unit proof showed the SES messaging provider boundary did not exist; red boundary proof showed no durable audit script. `npm run verify:tb-aws-localstack` starts Docker/LocalStack, confirms `s3=running` and `ses=running`, logs unclaimed provider statuses `cognito-idp=missing`, `textract=missing`, and `bedrock=missing`, runs the AWS SDK provider boundary audit, targeted S3/config/SES/Textract/OCR-mapping/Cognito unit proof, S3 receipt storage LocalStack smoke with Vitest `-t S3`, Cognito support classification smoke with Vitest `-t Cognito`, SES LocalStack smoke through `SesEmailProvider` with an injected LocalStack client, then `server:build`; final proof passed with 7 unit files / 55 tests, S3 smoke 2 passed / 1 skipped, Cognito classification 1 passed / 2 skipped, SES smoke 1 passed, and syntax check 103 files. LocalStack success is claimed only for S3 and SES; Textract, Cognito, and Bedrock local/live provider success remain explicit unclaimed gaps. |
| TB-INFRA-001 | Real AWS MVP infrastructure baseline provisions non-production RDS/PostgreSQL+PostGIS, private S3 receipts, Redis/BullMQ cache, ECR, ECS API/worker runtime, IAM/secrets, CloudWatch, deploy workflow, smoke proof, and rollback notes | yes | no | no | yes | in_progress | 2026-07-09 PR #41 Greptile/CI follow-up plus 2026-07-10 Redis secret closeout: API task role no longer grants `s3:DeleteObject` on receipt evidence; live Redis AUTH now requires explicit Terraform/workflow plaintext-in-state approval before plan/apply; CI static verifier runs Terraform Docker containers as the Linux runner UID/GID with `HOME=/tmp` to avoid root-owned temp cleanup `EACCES`; `REDIS_TLS=1` now enables TLS and `.env.example` documents `DATABASE_SSL_REJECT_UNAUTHORIZED`. Static workflow now runs both DB SSL and Redis TLS config tests, `DATABASE_SSL` rejects unknown values instead of silently disabling TLS, worker shutdown catches `disconnectDB()` errors, and API/worker ECS task definitions require both `database_secret_arn` and `redis_auth_secret_arn` before live planning. Static proof passed `actionlint`, `node --check`, targeted config unit proof 2 files / 9 tests, and `npm run verify:tb-infra-static` with Terraform fmt/init/validate/no-live plan, `server:build` 106 files, server Docker image build, and whitespace checks. Static proof only; no live `plan`, `apply`, AWS resource, image push, RDS migration, Redis/S3 smoke, ECS deploy, or runtime proof yet; spreadsheet task 1.2 remains not done. |
| TB-MOBILE-001 | Flutter mobile app has documented backend integration contracts before implementing OCR, GPS, reviews, and trust score flows | yes | yes | no | yes | in_progress | Runtime config, Cognito/local auth handoff, authenticated `GET /api/v1/users/me`, Flutter tests, and Android build proof exist. The 2026-07-13 contract refresh corrected stale restaurant/review/receipt endpoints; feature-specific UI proof remains separate. |
| TB-MOBILE-DISCOVER-001 | Flutter Discover loads public restaurant list/search/detail data from the Express API with location fallback and explicit UI states | yes | yes | yes | yes | implemented | 2026-07-14: strict DTO/query/repository proof, five-item suggestions, public requests omit auth state, location refresh, stale-response protection, preserved tab state, MOB-003 detail/reviews, and nullable `primaryImageUrl` rendering with deterministic backend selection and safe fallback. Harness verification passed 30 targeted mobile tests; full suites passed 321 server tests (2 skipped) and 61 mobile tests; `flutter analyze mobile`, server syntax, current migrations, and Android debug APK build passed. Emulator E2E confirmed one hosted image on the card and detail hero, then rolled back the temporary DB fixture. |
| TB-DATA-002 | Baseline PostgreSQL migration creates the documented TrustBite schema locally | no | yes | no | yes | implemented | `npm run docker:up`; `npm run db:migrate`; verified 54 application tables plus `schema_migrations` |

## Latest Static Evidence

- `TB-INFRA-001` 2026-07-09 PR #41 review follow-up plus 2026-07-10 Redis secret closeout: API receipt IAM is read/write only, Redis AUTH live plan/apply requires explicit plaintext-in-state approval, static verifier avoids Linux CI root-owned Docker temp files, config tests cover DB SSL plus Redis TLS including invalid SSL env rejection, API/worker ECS task definitions require DB and Redis secret injection before live planning, worker shutdown catches DB disconnect errors, and `DATABASE_SSL_REJECT_UNAUTHORIZED` is documented. Static checks were run only; no live AWS plan/apply, resource creation, ECS deploy, RDS migration, or runtime smoke proof is claimed.

## Evidence Rules

- Unit proof covers pure rules: Haversine distance, merchant similarity, timestamp limits, duplicate hash policy, trust-score math.
- Integration proof covers Express routes, PostgreSQL persistence, LocalStack/AWS service behavior, auth middleware, and API contracts.
- Database integration proof must include `npm run db:migrate` against local PostgreSQL plus insert/update rollback or reset evidence for the affected tables.
- E2E proof covers user-visible browser flows: search, select restaurant, upload receipt, review submission, auth-gated actions.
- Platform proof covers Docker/LocalStack/Postgres startup, environment setup, and runtime behavior that cannot be proven in lower layers.
- A story can be implemented without every proof column only when the story packet explains why.

## Required Proof By Work Type

| Work type | Minimum proof before completion |
| --- | --- |
| Docs-only harness changes | docs review + `npm run harness -- query ...` command where relevant |
| Client UI only | `npm run client:build` or documented failure; manual screenshot/E2E when behavior matters |
| Server route/service | server start/smoke plus integration or unit proof; add missing test script when practical |
| Database/schema | read `server/migrations/` and affected models; `npm run db:migrate`; local insert/update transaction proof; rollback/reset proof; data integrity check |
| Auth/security/provider | high-risk story, integration proof, negative-path proof, and durable decision if contract changes |
